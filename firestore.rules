rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only server-side IAM-authenticated Admin SDK is used.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
